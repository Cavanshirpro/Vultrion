0:UNKNOWN:Unknown key code. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>UNKNOWN</h1><h2>Keyevent Overview</h2><p>Unknown key code. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 0</code> and maps to the Android constant <code>KEYCODE_UNKNOWN</code>.</p><h2>Behavior Details</h2><p>Unknown key code This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
1:SOFT_LEFT:Soft Left key; usually the bottom-left software button. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SOFT_LEFT</h1><h2>Keyevent Overview</h2><p>Soft Left key; usually the bottom-left software button. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 1</code> and maps to the Android constant <code>KEYCODE_SOFT_LEFT</code>.</p><h2>Behavior Details</h2><p>Soft Left key; usually the bottom-left software button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
2:SOFT_RIGHT:Soft Right key; usually the bottom-right software button. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SOFT_RIGHT</h1><h2>Keyevent Overview</h2><p>Soft Right key; usually the bottom-right software button. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 2</code> and maps to the Android constant <code>KEYCODE_SOFT_RIGHT</code>.</p><h2>Behavior Details</h2><p>Soft Right key; usually the bottom-right software button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
3:HOME:Home key; usually returns to the home screen. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>HOME</h1><h2>Keyevent Overview</h2><p>Home key; usually returns to the home screen. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 3</code> and maps to the Android constant <code>KEYCODE_HOME</code>.</p><h2>Behavior Details</h2><p>Home key; usually returns to the home screen This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
4:BACK:Back key; usually navigates back. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>BACK</h1><h2>Keyevent Overview</h2><p>Back key; usually navigates back. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 4</code> and maps to the Android constant <code>KEYCODE_BACK</code>.</p><h2>Behavior Details</h2><p>Back key; usually navigates back This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
5:CALL:Call key; usually starts or answers a call. It is mainly used for playback or call-control workflows.:<h1>CALL</h1><h2>Keyevent Overview</h2><p>Call key; usually starts or answers a call. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 5</code> and maps to the Android constant <code>KEYCODE_CALL</code>.</p><h2>Behavior Details</h2><p>Call key; usually starts or answers a call This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
6:ENDCALL:End Call key; usually ends a call. It is mainly used for playback or call-control workflows.:<h1>ENDCALL</h1><h2>Keyevent Overview</h2><p>End Call key; usually ends a call. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 6</code> and maps to the Android constant <code>KEYCODE_ENDCALL</code>.</p><h2>Behavior Details</h2><p>End Call key; usually ends a call This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
7:0:Number 0 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>0</h1><h2>Keyevent Overview</h2><p>Number 0 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 7</code> and maps to the Android constant <code>KEYCODE_0</code>.</p><h2>Behavior Details</h2><p>Number 0 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
8:1:Number 1 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>1</h1><h2>Keyevent Overview</h2><p>Number 1 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 8</code> and maps to the Android constant <code>KEYCODE_1</code>.</p><h2>Behavior Details</h2><p>Number 1 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
9:2:Number 2 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>2</h1><h2>Keyevent Overview</h2><p>Number 2 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 9</code> and maps to the Android constant <code>KEYCODE_2</code>.</p><h2>Behavior Details</h2><p>Number 2 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
10:3:Number 3 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>3</h1><h2>Keyevent Overview</h2><p>Number 3 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 10</code> and maps to the Android constant <code>KEYCODE_3</code>.</p><h2>Behavior Details</h2><p>Number 3 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
11:4:Number 4 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>4</h1><h2>Keyevent Overview</h2><p>Number 4 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 11</code> and maps to the Android constant <code>KEYCODE_4</code>.</p><h2>Behavior Details</h2><p>Number 4 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
12:5:Number 5 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>5</h1><h2>Keyevent Overview</h2><p>Number 5 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 12</code> and maps to the Android constant <code>KEYCODE_5</code>.</p><h2>Behavior Details</h2><p>Number 5 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
13:6:Number 6 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>6</h1><h2>Keyevent Overview</h2><p>Number 6 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 13</code> and maps to the Android constant <code>KEYCODE_6</code>.</p><h2>Behavior Details</h2><p>Number 6 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
14:7:Number 7 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>7</h1><h2>Keyevent Overview</h2><p>Number 7 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 14</code> and maps to the Android constant <code>KEYCODE_7</code>.</p><h2>Behavior Details</h2><p>Number 7 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
15:8:Number 8 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>8</h1><h2>Keyevent Overview</h2><p>Number 8 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 15</code> and maps to the Android constant <code>KEYCODE_8</code>.</p><h2>Behavior Details</h2><p>Number 8 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
16:9:Number 9 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>9</h1><h2>Keyevent Overview</h2><p>Number 9 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 16</code> and maps to the Android constant <code>KEYCODE_9</code>.</p><h2>Behavior Details</h2><p>Number 9 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
17:STAR:Asterisk key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STAR</h1><h2>Keyevent Overview</h2><p>Asterisk key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 17</code> and maps to the Android constant <code>KEYCODE_STAR</code>.</p><h2>Behavior Details</h2><p>Asterisk key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
18:POUND:Hash / pound key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>POUND</h1><h2>Keyevent Overview</h2><p>Hash / pound key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 18</code> and maps to the Android constant <code>KEYCODE_POUND</code>.</p><h2>Behavior Details</h2><p>Hash / pound key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
19:DPAD_UP:Directional pad up key. It is used to move focus or navigate within interfaces.:<h1>DPAD_UP</h1><h2>Keyevent Overview</h2><p>Directional pad up key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 19</code> and maps to the Android constant <code>KEYCODE_DPAD_UP</code>.</p><h2>Behavior Details</h2><p>Directional pad up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
20:DPAD_DOWN:Directional pad down key. It is used to move focus or navigate within interfaces.:<h1>DPAD_DOWN</h1><h2>Keyevent Overview</h2><p>Directional pad down key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 20</code> and maps to the Android constant <code>KEYCODE_DPAD_DOWN</code>.</p><h2>Behavior Details</h2><p>Directional pad down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
21:DPAD_LEFT:Directional pad left key. It is used to move focus or navigate within interfaces.:<h1>DPAD_LEFT</h1><h2>Keyevent Overview</h2><p>Directional pad left key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 21</code> and maps to the Android constant <code>KEYCODE_DPAD_LEFT</code>.</p><h2>Behavior Details</h2><p>Directional pad left key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
22:DPAD_RIGHT:Directional pad right key. It is used to move focus or navigate within interfaces.:<h1>DPAD_RIGHT</h1><h2>Keyevent Overview</h2><p>Directional pad right key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 22</code> and maps to the Android constant <code>KEYCODE_DPAD_RIGHT</code>.</p><h2>Behavior Details</h2><p>Directional pad right key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
23:DPAD_CENTER:Directional pad center / select key. It is used to move focus or navigate within interfaces.:<h1>DPAD_CENTER</h1><h2>Keyevent Overview</h2><p>Directional pad center / select key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 23</code> and maps to the Android constant <code>KEYCODE_DPAD_CENTER</code>.</p><h2>Behavior Details</h2><p>Directional pad center / select key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
24:VOLUME_UP:Volume up key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VOLUME_UP</h1><h2>Keyevent Overview</h2><p>Volume up key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 24</code> and maps to the Android constant <code>KEYCODE_VOLUME_UP</code>.</p><h2>Behavior Details</h2><p>Volume up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
25:VOLUME_DOWN:Volume down key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VOLUME_DOWN</h1><h2>Keyevent Overview</h2><p>Volume down key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 25</code> and maps to the Android constant <code>KEYCODE_VOLUME_DOWN</code>.</p><h2>Behavior Details</h2><p>Volume down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
26:POWER:Power key. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>POWER</h1><h2>Keyevent Overview</h2><p>Power key. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 26</code> and maps to the Android constant <code>KEYCODE_POWER</code>.</p><h2>Behavior Details</h2><p>Power key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
27:CAMERA:Camera key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CAMERA</h1><h2>Keyevent Overview</h2><p>Camera key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 27</code> and maps to the Android constant <code>KEYCODE_CAMERA</code>.</p><h2>Behavior Details</h2><p>Camera key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
28:CLEAR:Clear key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CLEAR</h1><h2>Keyevent Overview</h2><p>Clear key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 28</code> and maps to the Android constant <code>KEYCODE_CLEAR</code>.</p><h2>Behavior Details</h2><p>Clear key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
29:A:Letter A key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>A</h1><h2>Keyevent Overview</h2><p>Letter A key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 29</code> and maps to the Android constant <code>KEYCODE_A</code>.</p><h2>Behavior Details</h2><p>Letter A key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
30:B:Letter B key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>B</h1><h2>Keyevent Overview</h2><p>Letter B key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 30</code> and maps to the Android constant <code>KEYCODE_B</code>.</p><h2>Behavior Details</h2><p>Letter B key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
31:C:Letter C key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>C</h1><h2>Keyevent Overview</h2><p>Letter C key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 31</code> and maps to the Android constant <code>KEYCODE_C</code>.</p><h2>Behavior Details</h2><p>Letter C key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
32:D:Letter D key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>D</h1><h2>Keyevent Overview</h2><p>Letter D key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 32</code> and maps to the Android constant <code>KEYCODE_D</code>.</p><h2>Behavior Details</h2><p>Letter D key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
33:E:Letter E key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>E</h1><h2>Keyevent Overview</h2><p>Letter E key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 33</code> and maps to the Android constant <code>KEYCODE_E</code>.</p><h2>Behavior Details</h2><p>Letter E key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
34:F:Letter F key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>F</h1><h2>Keyevent Overview</h2><p>Letter F key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 34</code> and maps to the Android constant <code>KEYCODE_F</code>.</p><h2>Behavior Details</h2><p>Letter F key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
35:G:Letter G key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>G</h1><h2>Keyevent Overview</h2><p>Letter G key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 35</code> and maps to the Android constant <code>KEYCODE_G</code>.</p><h2>Behavior Details</h2><p>Letter G key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
36:H:Letter H key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>H</h1><h2>Keyevent Overview</h2><p>Letter H key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 36</code> and maps to the Android constant <code>KEYCODE_H</code>.</p><h2>Behavior Details</h2><p>Letter H key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
37:I:Letter I key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>I</h1><h2>Keyevent Overview</h2><p>Letter I key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 37</code> and maps to the Android constant <code>KEYCODE_I</code>.</p><h2>Behavior Details</h2><p>Letter I key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
38:J:Letter J key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>J</h1><h2>Keyevent Overview</h2><p>Letter J key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 38</code> and maps to the Android constant <code>KEYCODE_J</code>.</p><h2>Behavior Details</h2><p>Letter J key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
39:K:Letter K key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>K</h1><h2>Keyevent Overview</h2><p>Letter K key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 39</code> and maps to the Android constant <code>KEYCODE_K</code>.</p><h2>Behavior Details</h2><p>Letter K key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
40:L:Letter L key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>L</h1><h2>Keyevent Overview</h2><p>Letter L key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 40</code> and maps to the Android constant <code>KEYCODE_L</code>.</p><h2>Behavior Details</h2><p>Letter L key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
41:M:Letter M key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>M</h1><h2>Keyevent Overview</h2><p>Letter M key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 41</code> and maps to the Android constant <code>KEYCODE_M</code>.</p><h2>Behavior Details</h2><p>Letter M key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
42:N:Letter N key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>N</h1><h2>Keyevent Overview</h2><p>Letter N key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 42</code> and maps to the Android constant <code>KEYCODE_N</code>.</p><h2>Behavior Details</h2><p>Letter N key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
43:O:Letter O key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>O</h1><h2>Keyevent Overview</h2><p>Letter O key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 43</code> and maps to the Android constant <code>KEYCODE_O</code>.</p><h2>Behavior Details</h2><p>Letter O key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
44:P:Letter P key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>P</h1><h2>Keyevent Overview</h2><p>Letter P key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 44</code> and maps to the Android constant <code>KEYCODE_P</code>.</p><h2>Behavior Details</h2><p>Letter P key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
45:Q:Letter Q key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>Q</h1><h2>Keyevent Overview</h2><p>Letter Q key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 45</code> and maps to the Android constant <code>KEYCODE_Q</code>.</p><h2>Behavior Details</h2><p>Letter Q key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
46:R:Letter R key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>R</h1><h2>Keyevent Overview</h2><p>Letter R key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 46</code> and maps to the Android constant <code>KEYCODE_R</code>.</p><h2>Behavior Details</h2><p>Letter R key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
47:S:Letter S key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>S</h1><h2>Keyevent Overview</h2><p>Letter S key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 47</code> and maps to the Android constant <code>KEYCODE_S</code>.</p><h2>Behavior Details</h2><p>Letter S key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
48:T:Letter T key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>T</h1><h2>Keyevent Overview</h2><p>Letter T key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 48</code> and maps to the Android constant <code>KEYCODE_T</code>.</p><h2>Behavior Details</h2><p>Letter T key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
49:U:Letter U key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>U</h1><h2>Keyevent Overview</h2><p>Letter U key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 49</code> and maps to the Android constant <code>KEYCODE_U</code>.</p><h2>Behavior Details</h2><p>Letter U key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
50:V:Letter V key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>V</h1><h2>Keyevent Overview</h2><p>Letter V key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 50</code> and maps to the Android constant <code>KEYCODE_V</code>.</p><h2>Behavior Details</h2><p>Letter V key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
51:W:Letter W key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>W</h1><h2>Keyevent Overview</h2><p>Letter W key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 51</code> and maps to the Android constant <code>KEYCODE_W</code>.</p><h2>Behavior Details</h2><p>Letter W key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
52:X:Letter X key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>X</h1><h2>Keyevent Overview</h2><p>Letter X key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 52</code> and maps to the Android constant <code>KEYCODE_X</code>.</p><h2>Behavior Details</h2><p>Letter X key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
53:Y:Letter Y key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>Y</h1><h2>Keyevent Overview</h2><p>Letter Y key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 53</code> and maps to the Android constant <code>KEYCODE_Y</code>.</p><h2>Behavior Details</h2><p>Letter Y key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
54:Z:Letter Z key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>Z</h1><h2>Keyevent Overview</h2><p>Letter Z key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 54</code> and maps to the Android constant <code>KEYCODE_Z</code>.</p><h2>Behavior Details</h2><p>Letter Z key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
55:COMMA:Comma key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>COMMA</h1><h2>Keyevent Overview</h2><p>Comma key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 55</code> and maps to the Android constant <code>KEYCODE_COMMA</code>.</p><h2>Behavior Details</h2><p>Comma key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
56:PERIOD:Period / dot key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PERIOD</h1><h2>Keyevent Overview</h2><p>Period / dot key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 56</code> and maps to the Android constant <code>KEYCODE_PERIOD</code>.</p><h2>Behavior Details</h2><p>Period / dot key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
57:ALT_LEFT:Left Alt modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>ALT_LEFT</h1><h2>Keyevent Overview</h2><p>Left Alt modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 57</code> and maps to the Android constant <code>KEYCODE_ALT_LEFT</code>.</p><h2>Behavior Details</h2><p>Left Alt modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
58:ALT_RIGHT:Right Alt modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>ALT_RIGHT</h1><h2>Keyevent Overview</h2><p>Right Alt modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 58</code> and maps to the Android constant <code>KEYCODE_ALT_RIGHT</code>.</p><h2>Behavior Details</h2><p>Right Alt modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
59:SHIFT_LEFT:Left Shift modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>SHIFT_LEFT</h1><h2>Keyevent Overview</h2><p>Left Shift modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 59</code> and maps to the Android constant <code>KEYCODE_SHIFT_LEFT</code>.</p><h2>Behavior Details</h2><p>Left Shift modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
60:SHIFT_RIGHT:Right Shift modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>SHIFT_RIGHT</h1><h2>Keyevent Overview</h2><p>Right Shift modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 60</code> and maps to the Android constant <code>KEYCODE_SHIFT_RIGHT</code>.</p><h2>Behavior Details</h2><p>Right Shift modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
61:TAB:Tab key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>TAB</h1><h2>Keyevent Overview</h2><p>Tab key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 61</code> and maps to the Android constant <code>KEYCODE_TAB</code>.</p><h2>Behavior Details</h2><p>Tab key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
62:SPACE:Space key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SPACE</h1><h2>Keyevent Overview</h2><p>Space key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 62</code> and maps to the Android constant <code>KEYCODE_SPACE</code>.</p><h2>Behavior Details</h2><p>Space key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
63:SYM:Symbol modifier key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SYM</h1><h2>Keyevent Overview</h2><p>Symbol modifier key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 63</code> and maps to the Android constant <code>KEYCODE_SYM</code>.</p><h2>Behavior Details</h2><p>Symbol modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
64:EXPLORER:Browser / explorer key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>EXPLORER</h1><h2>Keyevent Overview</h2><p>Browser / explorer key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 64</code> and maps to the Android constant <code>KEYCODE_EXPLORER</code>.</p><h2>Behavior Details</h2><p>Browser / explorer key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
65:ENVELOPE:Mail / envelope key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ENVELOPE</h1><h2>Keyevent Overview</h2><p>Mail / envelope key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 65</code> and maps to the Android constant <code>KEYCODE_ENVELOPE</code>.</p><h2>Behavior Details</h2><p>Mail / envelope key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
66:ENTER:Enter key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ENTER</h1><h2>Keyevent Overview</h2><p>Enter key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 66</code> and maps to the Android constant <code>KEYCODE_ENTER</code>.</p><h2>Behavior Details</h2><p>Enter key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
67:DEL:Backspace / delete-left key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>DEL</h1><h2>Keyevent Overview</h2><p>Backspace / delete-left key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 67</code> and maps to the Android constant <code>KEYCODE_DEL</code>.</p><h2>Behavior Details</h2><p>Backspace / delete-left key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
68:GRAVE:Backtick key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>GRAVE</h1><h2>Keyevent Overview</h2><p>Backtick key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 68</code> and maps to the Android constant <code>KEYCODE_GRAVE</code>.</p><h2>Behavior Details</h2><p>Backtick key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
69:MINUS:Minus key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MINUS</h1><h2>Keyevent Overview</h2><p>Minus key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 69</code> and maps to the Android constant <code>KEYCODE_MINUS</code>.</p><h2>Behavior Details</h2><p>Minus key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
70:EQUALS:Equals key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>EQUALS</h1><h2>Keyevent Overview</h2><p>Equals key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 70</code> and maps to the Android constant <code>KEYCODE_EQUALS</code>.</p><h2>Behavior Details</h2><p>Equals key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
71:LEFT_BRACKET:Left bracket key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>LEFT_BRACKET</h1><h2>Keyevent Overview</h2><p>Left bracket key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 71</code> and maps to the Android constant <code>KEYCODE_LEFT_BRACKET</code>.</p><h2>Behavior Details</h2><p>Left bracket key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
72:RIGHT_BRACKET:Right bracket key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>RIGHT_BRACKET</h1><h2>Keyevent Overview</h2><p>Right bracket key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 72</code> and maps to the Android constant <code>KEYCODE_RIGHT_BRACKET</code>.</p><h2>Behavior Details</h2><p>Right bracket key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
73:BACKSLASH:Backslash key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>BACKSLASH</h1><h2>Keyevent Overview</h2><p>Backslash key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 73</code> and maps to the Android constant <code>KEYCODE_BACKSLASH</code>.</p><h2>Behavior Details</h2><p>Backslash key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
74:SEMICOLON:Semicolon key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SEMICOLON</h1><h2>Keyevent Overview</h2><p>Semicolon key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 74</code> and maps to the Android constant <code>KEYCODE_SEMICOLON</code>.</p><h2>Behavior Details</h2><p>Semicolon key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
75:APOSTROPHE:Apostrophe key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>APOSTROPHE</h1><h2>Keyevent Overview</h2><p>Apostrophe key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 75</code> and maps to the Android constant <code>KEYCODE_APOSTROPHE</code>.</p><h2>Behavior Details</h2><p>Apostrophe key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
76:SLASH:Slash key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SLASH</h1><h2>Keyevent Overview</h2><p>Slash key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 76</code> and maps to the Android constant <code>KEYCODE_SLASH</code>.</p><h2>Behavior Details</h2><p>Slash key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
77:AT:At sign key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>AT</h1><h2>Keyevent Overview</h2><p>At sign key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 77</code> and maps to the Android constant <code>KEYCODE_AT</code>.</p><h2>Behavior Details</h2><p>At sign key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
78:NUM:Number modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>NUM</h1><h2>Keyevent Overview</h2><p>Number modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 78</code> and maps to the Android constant <code>KEYCODE_NUM</code>.</p><h2>Behavior Details</h2><p>Number modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
79:HEADSETHOOK:Headset hook key. It is mainly used for playback or call-control workflows.:<h1>HEADSETHOOK</h1><h2>Keyevent Overview</h2><p>Headset hook key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 79</code> and maps to the Android constant <code>KEYCODE_HEADSETHOOK</code>.</p><h2>Behavior Details</h2><p>Headset hook key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
80:FOCUS:Camera focus key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FOCUS</h1><h2>Keyevent Overview</h2><p>Camera focus key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 80</code> and maps to the Android constant <code>KEYCODE_FOCUS</code>.</p><h2>Behavior Details</h2><p>Camera focus key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
81:PLUS:Plus key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PLUS</h1><h2>Keyevent Overview</h2><p>Plus key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 81</code> and maps to the Android constant <code>KEYCODE_PLUS</code>.</p><h2>Behavior Details</h2><p>Plus key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
82:MENU:Menu key. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>MENU</h1><h2>Keyevent Overview</h2><p>Menu key. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 82</code> and maps to the Android constant <code>KEYCODE_MENU</code>.</p><h2>Behavior Details</h2><p>Menu key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
83:NOTIFICATION:Notification key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>NOTIFICATION</h1><h2>Keyevent Overview</h2><p>Notification key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 83</code> and maps to the Android constant <code>KEYCODE_NOTIFICATION</code>.</p><h2>Behavior Details</h2><p>Notification key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
84:SEARCH:Search key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SEARCH</h1><h2>Keyevent Overview</h2><p>Search key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 84</code> and maps to the Android constant <code>KEYCODE_SEARCH</code>.</p><h2>Behavior Details</h2><p>Search key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
85:MEDIA_PLAY_PAUSE:Play or pause media. It is mainly used for playback or call-control workflows.:<h1>MEDIA_PLAY_PAUSE</h1><h2>Keyevent Overview</h2><p>Play or pause media. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 85</code> and maps to the Android constant <code>KEYCODE_MEDIA_PLAY_PAUSE</code>.</p><h2>Behavior Details</h2><p>Play or pause media This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
86:MEDIA_STOP:Stop media playback. It is mainly used for playback or call-control workflows.:<h1>MEDIA_STOP</h1><h2>Keyevent Overview</h2><p>Stop media playback. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 86</code> and maps to the Android constant <code>KEYCODE_MEDIA_STOP</code>.</p><h2>Behavior Details</h2><p>Stop media playback This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
87:MEDIA_NEXT:Play next media item. It is mainly used for playback or call-control workflows.:<h1>MEDIA_NEXT</h1><h2>Keyevent Overview</h2><p>Play next media item. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 87</code> and maps to the Android constant <code>KEYCODE_MEDIA_NEXT</code>.</p><h2>Behavior Details</h2><p>Play next media item This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
88:MEDIA_PREVIOUS:Play previous media item. It is mainly used for playback or call-control workflows.:<h1>MEDIA_PREVIOUS</h1><h2>Keyevent Overview</h2><p>Play previous media item. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 88</code> and maps to the Android constant <code>KEYCODE_MEDIA_PREVIOUS</code>.</p><h2>Behavior Details</h2><p>Play previous media item This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
89:MEDIA_REWIND:Rewind media. It is mainly used for playback or call-control workflows.:<h1>MEDIA_REWIND</h1><h2>Keyevent Overview</h2><p>Rewind media. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 89</code> and maps to the Android constant <code>KEYCODE_MEDIA_REWIND</code>.</p><h2>Behavior Details</h2><p>Rewind media This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
90:MEDIA_FAST_FORWARD:Fast-forward media. It is mainly used for playback or call-control workflows.:<h1>MEDIA_FAST_FORWARD</h1><h2>Keyevent Overview</h2><p>Fast-forward media. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 90</code> and maps to the Android constant <code>KEYCODE_MEDIA_FAST_FORWARD</code>.</p><h2>Behavior Details</h2><p>Fast-forward media This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
91:MUTE:Microphone mute key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MUTE</h1><h2>Keyevent Overview</h2><p>Microphone mute key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 91</code> and maps to the Android constant <code>KEYCODE_MUTE</code>.</p><h2>Behavior Details</h2><p>Microphone mute key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
92:PAGE_UP:Page up key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PAGE_UP</h1><h2>Keyevent Overview</h2><p>Page up key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 92</code> and maps to the Android constant <code>KEYCODE_PAGE_UP</code>.</p><h2>Behavior Details</h2><p>Page up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
93:PAGE_DOWN:Page down key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PAGE_DOWN</h1><h2>Keyevent Overview</h2><p>Page down key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 93</code> and maps to the Android constant <code>KEYCODE_PAGE_DOWN</code>.</p><h2>Behavior Details</h2><p>Page down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
94:PICTSYMBOLS:Picture-symbols modifier key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PICTSYMBOLS</h1><h2>Keyevent Overview</h2><p>Picture-symbols modifier key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 94</code> and maps to the Android constant <code>KEYCODE_PICTSYMBOLS</code>.</p><h2>Behavior Details</h2><p>Picture-symbols modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
95:SWITCH_CHARSET:Charset switching modifier key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SWITCH_CHARSET</h1><h2>Keyevent Overview</h2><p>Charset switching modifier key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 95</code> and maps to the Android constant <code>KEYCODE_SWITCH_CHARSET</code>.</p><h2>Behavior Details</h2><p>Charset switching modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
96:BUTTON_A:Game controller A button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_A</h1><h2>Keyevent Overview</h2><p>Game controller A button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 96</code> and maps to the Android constant <code>KEYCODE_BUTTON_A</code>.</p><h2>Behavior Details</h2><p>Game controller A button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
97:BUTTON_B:Game controller B button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_B</h1><h2>Keyevent Overview</h2><p>Game controller B button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 97</code> and maps to the Android constant <code>KEYCODE_BUTTON_B</code>.</p><h2>Behavior Details</h2><p>Game controller B button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
98:BUTTON_C:Game controller C button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_C</h1><h2>Keyevent Overview</h2><p>Game controller C button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 98</code> and maps to the Android constant <code>KEYCODE_BUTTON_C</code>.</p><h2>Behavior Details</h2><p>Game controller C button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
99:BUTTON_X:Game controller X button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_X</h1><h2>Keyevent Overview</h2><p>Game controller X button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 99</code> and maps to the Android constant <code>KEYCODE_BUTTON_X</code>.</p><h2>Behavior Details</h2><p>Game controller X button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
100:BUTTON_Y:Game controller Y button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_Y</h1><h2>Keyevent Overview</h2><p>Game controller Y button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 100</code> and maps to the Android constant <code>KEYCODE_BUTTON_Y</code>.</p><h2>Behavior Details</h2><p>Game controller Y button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
101:BUTTON_Z:Game controller Z button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_Z</h1><h2>Keyevent Overview</h2><p>Game controller Z button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 101</code> and maps to the Android constant <code>KEYCODE_BUTTON_Z</code>.</p><h2>Behavior Details</h2><p>Game controller Z button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
102:BUTTON_L1:Game controller L1 button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_L1</h1><h2>Keyevent Overview</h2><p>Game controller L1 button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 102</code> and maps to the Android constant <code>KEYCODE_BUTTON_L1</code>.</p><h2>Behavior Details</h2><p>Game controller L1 button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
103:BUTTON_R1:Game controller R1 button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_R1</h1><h2>Keyevent Overview</h2><p>Game controller R1 button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 103</code> and maps to the Android constant <code>KEYCODE_BUTTON_R1</code>.</p><h2>Behavior Details</h2><p>Game controller R1 button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
104:BUTTON_L2:Game controller L2 button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_L2</h1><h2>Keyevent Overview</h2><p>Game controller L2 button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 104</code> and maps to the Android constant <code>KEYCODE_BUTTON_L2</code>.</p><h2>Behavior Details</h2><p>Game controller L2 button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
105:BUTTON_R2:Game controller R2 button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_R2</h1><h2>Keyevent Overview</h2><p>Game controller R2 button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 105</code> and maps to the Android constant <code>KEYCODE_BUTTON_R2</code>.</p><h2>Behavior Details</h2><p>Game controller R2 button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
106:BUTTON_THUMBL:Left thumbstick press button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_THUMBL</h1><h2>Keyevent Overview</h2><p>Left thumbstick press button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 106</code> and maps to the Android constant <code>KEYCODE_BUTTON_THUMBL</code>.</p><h2>Behavior Details</h2><p>Left thumbstick press button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
107:BUTTON_THUMBR:Right thumbstick press button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_THUMBR</h1><h2>Keyevent Overview</h2><p>Right thumbstick press button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 107</code> and maps to the Android constant <code>KEYCODE_BUTTON_THUMBR</code>.</p><h2>Behavior Details</h2><p>Right thumbstick press button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
108:BUTTON_START:Game controller Start button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_START</h1><h2>Keyevent Overview</h2><p>Game controller Start button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 108</code> and maps to the Android constant <code>KEYCODE_BUTTON_START</code>.</p><h2>Behavior Details</h2><p>Game controller Start button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
109:BUTTON_SELECT:Game controller Select button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_SELECT</h1><h2>Keyevent Overview</h2><p>Game controller Select button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 109</code> and maps to the Android constant <code>KEYCODE_BUTTON_SELECT</code>.</p><h2>Behavior Details</h2><p>Game controller Select button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
110:BUTTON_MODE:Game controller Mode button. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_MODE</h1><h2>Keyevent Overview</h2><p>Game controller Mode button. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 110</code> and maps to the Android constant <code>KEYCODE_BUTTON_MODE</code>.</p><h2>Behavior Details</h2><p>Game controller Mode button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
111:ESCAPE:Escape key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ESCAPE</h1><h2>Keyevent Overview</h2><p>Escape key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 111</code> and maps to the Android constant <code>KEYCODE_ESCAPE</code>.</p><h2>Behavior Details</h2><p>Escape key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
112:FORWARD_DEL:Forward delete key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FORWARD_DEL</h1><h2>Keyevent Overview</h2><p>Forward delete key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 112</code> and maps to the Android constant <code>KEYCODE_FORWARD_DEL</code>.</p><h2>Behavior Details</h2><p>Forward delete key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
113:CTRL_LEFT:Left Control modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>CTRL_LEFT</h1><h2>Keyevent Overview</h2><p>Left Control modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 113</code> and maps to the Android constant <code>KEYCODE_CTRL_LEFT</code>.</p><h2>Behavior Details</h2><p>Left Control modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
114:CTRL_RIGHT:Right Control modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>CTRL_RIGHT</h1><h2>Keyevent Overview</h2><p>Right Control modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 114</code> and maps to the Android constant <code>KEYCODE_CTRL_RIGHT</code>.</p><h2>Behavior Details</h2><p>Right Control modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
115:CAPS_LOCK:Caps Lock key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CAPS_LOCK</h1><h2>Keyevent Overview</h2><p>Caps Lock key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 115</code> and maps to the Android constant <code>KEYCODE_CAPS_LOCK</code>.</p><h2>Behavior Details</h2><p>Caps Lock key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
116:SCROLL_LOCK:Scroll Lock key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SCROLL_LOCK</h1><h2>Keyevent Overview</h2><p>Scroll Lock key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 116</code> and maps to the Android constant <code>KEYCODE_SCROLL_LOCK</code>.</p><h2>Behavior Details</h2><p>Scroll Lock key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
117:META_LEFT:Left Meta / Windows key. It acts as a modifier key and is typically combined with other keys.:<h1>META_LEFT</h1><h2>Keyevent Overview</h2><p>Left Meta / Windows key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 117</code> and maps to the Android constant <code>KEYCODE_META_LEFT</code>.</p><h2>Behavior Details</h2><p>Left Meta / Windows key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
118:META_RIGHT:Right Meta / Windows key. It acts as a modifier key and is typically combined with other keys.:<h1>META_RIGHT</h1><h2>Keyevent Overview</h2><p>Right Meta / Windows key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 118</code> and maps to the Android constant <code>KEYCODE_META_RIGHT</code>.</p><h2>Behavior Details</h2><p>Right Meta / Windows key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
119:FUNCTION:Function modifier key. It acts as a modifier key and is typically combined with other keys.:<h1>FUNCTION</h1><h2>Keyevent Overview</h2><p>Function modifier key. It acts as a modifier key and is typically combined with other keys.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 119</code> and maps to the Android constant <code>KEYCODE_FUNCTION</code>.</p><h2>Behavior Details</h2><p>Function modifier key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
120:SYSRQ:SysRq / Print Screen key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SYSRQ</h1><h2>Keyevent Overview</h2><p>SysRq / Print Screen key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 120</code> and maps to the Android constant <code>KEYCODE_SYSRQ</code>.</p><h2>Behavior Details</h2><p>SysRq / Print Screen key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
121:BREAK:Break key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>BREAK</h1><h2>Keyevent Overview</h2><p>Break key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 121</code> and maps to the Android constant <code>KEYCODE_BREAK</code>.</p><h2>Behavior Details</h2><p>Break key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
122:MOVE_HOME:Move to home / start key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MOVE_HOME</h1><h2>Keyevent Overview</h2><p>Move to home / start key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 122</code> and maps to the Android constant <code>KEYCODE_MOVE_HOME</code>.</p><h2>Behavior Details</h2><p>Move to home / start key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
123:MOVE_END:Move to end key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MOVE_END</h1><h2>Keyevent Overview</h2><p>Move to end key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 123</code> and maps to the Android constant <code>KEYCODE_MOVE_END</code>.</p><h2>Behavior Details</h2><p>Move to end key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
124:INSERT:Insert key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>INSERT</h1><h2>Keyevent Overview</h2><p>Insert key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 124</code> and maps to the Android constant <code>KEYCODE_INSERT</code>.</p><h2>Behavior Details</h2><p>Insert key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
125:FORWARD:Forward navigation key. It is used to move focus or navigate within interfaces.:<h1>FORWARD</h1><h2>Keyevent Overview</h2><p>Forward navigation key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 125</code> and maps to the Android constant <code>KEYCODE_FORWARD</code>.</p><h2>Behavior Details</h2><p>Forward navigation key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
126:MEDIA_PLAY:Play media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_PLAY</h1><h2>Keyevent Overview</h2><p>Play media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 126</code> and maps to the Android constant <code>KEYCODE_MEDIA_PLAY</code>.</p><h2>Behavior Details</h2><p>Play media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
127:MEDIA_PAUSE:Pause media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_PAUSE</h1><h2>Keyevent Overview</h2><p>Pause media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 127</code> and maps to the Android constant <code>KEYCODE_MEDIA_PAUSE</code>.</p><h2>Behavior Details</h2><p>Pause media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
128:MEDIA_CLOSE:Close media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_CLOSE</h1><h2>Keyevent Overview</h2><p>Close media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 128</code> and maps to the Android constant <code>KEYCODE_MEDIA_CLOSE</code>.</p><h2>Behavior Details</h2><p>Close media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
129:MEDIA_EJECT:Eject media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_EJECT</h1><h2>Keyevent Overview</h2><p>Eject media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 129</code> and maps to the Android constant <code>KEYCODE_MEDIA_EJECT</code>.</p><h2>Behavior Details</h2><p>Eject media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
130:MEDIA_RECORD:Record media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_RECORD</h1><h2>Keyevent Overview</h2><p>Record media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 130</code> and maps to the Android constant <code>KEYCODE_MEDIA_RECORD</code>.</p><h2>Behavior Details</h2><p>Record media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
131:F1:Function key F1. It behaves like a standard function key on external keyboards.:<h1>F1</h1><h2>Keyevent Overview</h2><p>Function key F1. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 131</code> and maps to the Android constant <code>KEYCODE_F1</code>.</p><h2>Behavior Details</h2><p>Function key F1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
132:F2:Function key F2. It behaves like a standard function key on external keyboards.:<h1>F2</h1><h2>Keyevent Overview</h2><p>Function key F2. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 132</code> and maps to the Android constant <code>KEYCODE_F2</code>.</p><h2>Behavior Details</h2><p>Function key F2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
133:F3:Function key F3. It behaves like a standard function key on external keyboards.:<h1>F3</h1><h2>Keyevent Overview</h2><p>Function key F3. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 133</code> and maps to the Android constant <code>KEYCODE_F3</code>.</p><h2>Behavior Details</h2><p>Function key F3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
134:F4:Function key F4. It behaves like a standard function key on external keyboards.:<h1>F4</h1><h2>Keyevent Overview</h2><p>Function key F4. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 134</code> and maps to the Android constant <code>KEYCODE_F4</code>.</p><h2>Behavior Details</h2><p>Function key F4 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
135:F5:Function key F5. It behaves like a standard function key on external keyboards.:<h1>F5</h1><h2>Keyevent Overview</h2><p>Function key F5. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 135</code> and maps to the Android constant <code>KEYCODE_F5</code>.</p><h2>Behavior Details</h2><p>Function key F5 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
136:F6:Function key F6. It behaves like a standard function key on external keyboards.:<h1>F6</h1><h2>Keyevent Overview</h2><p>Function key F6. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 136</code> and maps to the Android constant <code>KEYCODE_F6</code>.</p><h2>Behavior Details</h2><p>Function key F6 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
137:F7:Function key F7. It behaves like a standard function key on external keyboards.:<h1>F7</h1><h2>Keyevent Overview</h2><p>Function key F7. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 137</code> and maps to the Android constant <code>KEYCODE_F7</code>.</p><h2>Behavior Details</h2><p>Function key F7 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
138:F8:Function key F8. It behaves like a standard function key on external keyboards.:<h1>F8</h1><h2>Keyevent Overview</h2><p>Function key F8. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 138</code> and maps to the Android constant <code>KEYCODE_F8</code>.</p><h2>Behavior Details</h2><p>Function key F8 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
139:F9:Function key F9. It behaves like a standard function key on external keyboards.:<h1>F9</h1><h2>Keyevent Overview</h2><p>Function key F9. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 139</code> and maps to the Android constant <code>KEYCODE_F9</code>.</p><h2>Behavior Details</h2><p>Function key F9 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
140:F10:Function key F10. It behaves like a standard function key on external keyboards.:<h1>F10</h1><h2>Keyevent Overview</h2><p>Function key F10. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 140</code> and maps to the Android constant <code>KEYCODE_F10</code>.</p><h2>Behavior Details</h2><p>Function key F10 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
141:F11:Function key F11. It behaves like a standard function key on external keyboards.:<h1>F11</h1><h2>Keyevent Overview</h2><p>Function key F11. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 141</code> and maps to the Android constant <code>KEYCODE_F11</code>.</p><h2>Behavior Details</h2><p>Function key F11 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
142:F12:Function key F12. It behaves like a standard function key on external keyboards.:<h1>F12</h1><h2>Keyevent Overview</h2><p>Function key F12. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 142</code> and maps to the Android constant <code>KEYCODE_F12</code>.</p><h2>Behavior Details</h2><p>Function key F12 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
143:NUM_LOCK:Num Lock key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>NUM_LOCK</h1><h2>Keyevent Overview</h2><p>Num Lock key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 143</code> and maps to the Android constant <code>KEYCODE_NUM_LOCK</code>.</p><h2>Behavior Details</h2><p>Num Lock key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
144:NUMPAD_0:Numeric keypad 0 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_0</h1><h2>Keyevent Overview</h2><p>Numeric keypad 0 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 144</code> and maps to the Android constant <code>KEYCODE_NUMPAD_0</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 0 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
145:NUMPAD_1:Numeric keypad 1 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_1</h1><h2>Keyevent Overview</h2><p>Numeric keypad 1 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 145</code> and maps to the Android constant <code>KEYCODE_NUMPAD_1</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 1 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
146:NUMPAD_2:Numeric keypad 2 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_2</h1><h2>Keyevent Overview</h2><p>Numeric keypad 2 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 146</code> and maps to the Android constant <code>KEYCODE_NUMPAD_2</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 2 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
147:NUMPAD_3:Numeric keypad 3 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_3</h1><h2>Keyevent Overview</h2><p>Numeric keypad 3 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 147</code> and maps to the Android constant <code>KEYCODE_NUMPAD_3</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 3 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
148:NUMPAD_4:Numeric keypad 4 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_4</h1><h2>Keyevent Overview</h2><p>Numeric keypad 4 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 148</code> and maps to the Android constant <code>KEYCODE_NUMPAD_4</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 4 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
149:NUMPAD_5:Numeric keypad 5 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_5</h1><h2>Keyevent Overview</h2><p>Numeric keypad 5 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 149</code> and maps to the Android constant <code>KEYCODE_NUMPAD_5</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 5 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
150:NUMPAD_6:Numeric keypad 6 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_6</h1><h2>Keyevent Overview</h2><p>Numeric keypad 6 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 150</code> and maps to the Android constant <code>KEYCODE_NUMPAD_6</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 6 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
151:NUMPAD_7:Numeric keypad 7 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_7</h1><h2>Keyevent Overview</h2><p>Numeric keypad 7 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 151</code> and maps to the Android constant <code>KEYCODE_NUMPAD_7</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 7 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
152:NUMPAD_8:Numeric keypad 8 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_8</h1><h2>Keyevent Overview</h2><p>Numeric keypad 8 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 152</code> and maps to the Android constant <code>KEYCODE_NUMPAD_8</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 8 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
153:NUMPAD_9:Numeric keypad 9 key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_9</h1><h2>Keyevent Overview</h2><p>Numeric keypad 9 key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 153</code> and maps to the Android constant <code>KEYCODE_NUMPAD_9</code>.</p><h2>Behavior Details</h2><p>Numeric keypad 9 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
154:NUMPAD_DIVIDE:Numeric keypad divide key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_DIVIDE</h1><h2>Keyevent Overview</h2><p>Numeric keypad divide key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 154</code> and maps to the Android constant <code>KEYCODE_NUMPAD_DIVIDE</code>.</p><h2>Behavior Details</h2><p>Numeric keypad divide key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
155:NUMPAD_MULTIPLY:Numeric keypad multiply key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_MULTIPLY</h1><h2>Keyevent Overview</h2><p>Numeric keypad multiply key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 155</code> and maps to the Android constant <code>KEYCODE_NUMPAD_MULTIPLY</code>.</p><h2>Behavior Details</h2><p>Numeric keypad multiply key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
156:NUMPAD_SUBTRACT:Numeric keypad subtract key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_SUBTRACT</h1><h2>Keyevent Overview</h2><p>Numeric keypad subtract key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 156</code> and maps to the Android constant <code>KEYCODE_NUMPAD_SUBTRACT</code>.</p><h2>Behavior Details</h2><p>Numeric keypad subtract key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
157:NUMPAD_ADD:Numeric keypad add key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_ADD</h1><h2>Keyevent Overview</h2><p>Numeric keypad add key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 157</code> and maps to the Android constant <code>KEYCODE_NUMPAD_ADD</code>.</p><h2>Behavior Details</h2><p>Numeric keypad add key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
158:NUMPAD_DOT:Numeric keypad dot / decimal key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_DOT</h1><h2>Keyevent Overview</h2><p>Numeric keypad dot / decimal key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 158</code> and maps to the Android constant <code>KEYCODE_NUMPAD_DOT</code>.</p><h2>Behavior Details</h2><p>Numeric keypad dot / decimal key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
159:NUMPAD_COMMA:Numeric keypad comma key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_COMMA</h1><h2>Keyevent Overview</h2><p>Numeric keypad comma key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 159</code> and maps to the Android constant <code>KEYCODE_NUMPAD_COMMA</code>.</p><h2>Behavior Details</h2><p>Numeric keypad comma key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
160:NUMPAD_ENTER:Numeric keypad enter key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_ENTER</h1><h2>Keyevent Overview</h2><p>Numeric keypad enter key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 160</code> and maps to the Android constant <code>KEYCODE_NUMPAD_ENTER</code>.</p><h2>Behavior Details</h2><p>Numeric keypad enter key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
161:NUMPAD_EQUALS:Numeric keypad equals key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_EQUALS</h1><h2>Keyevent Overview</h2><p>Numeric keypad equals key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 161</code> and maps to the Android constant <code>KEYCODE_NUMPAD_EQUALS</code>.</p><h2>Behavior Details</h2><p>Numeric keypad equals key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
162:NUMPAD_LEFT_PAREN:Numeric keypad left parenthesis key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_LEFT_PAREN</h1><h2>Keyevent Overview</h2><p>Numeric keypad left parenthesis key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 162</code> and maps to the Android constant <code>KEYCODE_NUMPAD_LEFT_PAREN</code>.</p><h2>Behavior Details</h2><p>Numeric keypad left parenthesis key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
163:NUMPAD_RIGHT_PAREN:Numeric keypad right parenthesis key. It targets the numeric keypad rather than the main keyboard row.:<h1>NUMPAD_RIGHT_PAREN</h1><h2>Keyevent Overview</h2><p>Numeric keypad right parenthesis key. It targets the numeric keypad rather than the main keyboard row.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 163</code> and maps to the Android constant <code>KEYCODE_NUMPAD_RIGHT_PAREN</code>.</p><h2>Behavior Details</h2><p>Numeric keypad right parenthesis key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
164:VOLUME_MUTE:Speaker mute key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VOLUME_MUTE</h1><h2>Keyevent Overview</h2><p>Speaker mute key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 164</code> and maps to the Android constant <code>KEYCODE_VOLUME_MUTE</code>.</p><h2>Behavior Details</h2><p>Speaker mute key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
165:INFO:Info key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>INFO</h1><h2>Keyevent Overview</h2><p>Info key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 165</code> and maps to the Android constant <code>KEYCODE_INFO</code>.</p><h2>Behavior Details</h2><p>Info key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
166:CHANNEL_UP:Channel up key. It is commonly found on TV remotes and set-top-box style devices.:<h1>CHANNEL_UP</h1><h2>Keyevent Overview</h2><p>Channel up key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 166</code> and maps to the Android constant <code>KEYCODE_CHANNEL_UP</code>.</p><h2>Behavior Details</h2><p>Channel up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
167:CHANNEL_DOWN:Channel down key. It is commonly found on TV remotes and set-top-box style devices.:<h1>CHANNEL_DOWN</h1><h2>Keyevent Overview</h2><p>Channel down key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 167</code> and maps to the Android constant <code>KEYCODE_CHANNEL_DOWN</code>.</p><h2>Behavior Details</h2><p>Channel down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
168:ZOOM_IN:Zoom in key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ZOOM_IN</h1><h2>Keyevent Overview</h2><p>Zoom in key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 168</code> and maps to the Android constant <code>KEYCODE_ZOOM_IN</code>.</p><h2>Behavior Details</h2><p>Zoom in key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
169:ZOOM_OUT:Zoom out key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ZOOM_OUT</h1><h2>Keyevent Overview</h2><p>Zoom out key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 169</code> and maps to the Android constant <code>KEYCODE_ZOOM_OUT</code>.</p><h2>Behavior Details</h2><p>Zoom out key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
170:TV:TV key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>TV</h1><h2>Keyevent Overview</h2><p>TV key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 170</code> and maps to the Android constant <code>KEYCODE_TV</code>.</p><h2>Behavior Details</h2><p>TV key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
171:WINDOW:Window / picture-in-picture key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>WINDOW</h1><h2>Keyevent Overview</h2><p>Window / picture-in-picture key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 171</code> and maps to the Android constant <code>KEYCODE_WINDOW</code>.</p><h2>Behavior Details</h2><p>Window / picture-in-picture key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
172:GUIDE:Guide key. It is commonly found on TV remotes and set-top-box style devices.:<h1>GUIDE</h1><h2>Keyevent Overview</h2><p>Guide key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 172</code> and maps to the Android constant <code>KEYCODE_GUIDE</code>.</p><h2>Behavior Details</h2><p>Guide key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
173:DVR:DVR key. It is commonly found on TV remotes and set-top-box style devices.:<h1>DVR</h1><h2>Keyevent Overview</h2><p>DVR key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 173</code> and maps to the Android constant <code>KEYCODE_DVR</code>.</p><h2>Behavior Details</h2><p>DVR key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
174:BOOKMARK:Bookmark key. It is commonly found on TV remotes and set-top-box style devices.:<h1>BOOKMARK</h1><h2>Keyevent Overview</h2><p>Bookmark key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 174</code> and maps to the Android constant <code>KEYCODE_BOOKMARK</code>.</p><h2>Behavior Details</h2><p>Bookmark key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
175:CAPTIONS:Captions key. It is commonly found on TV remotes and set-top-box style devices.:<h1>CAPTIONS</h1><h2>Keyevent Overview</h2><p>Captions key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 175</code> and maps to the Android constant <code>KEYCODE_CAPTIONS</code>.</p><h2>Behavior Details</h2><p>Captions key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
176:SETTINGS:Settings key. It is commonly found on TV remotes and set-top-box style devices.:<h1>SETTINGS</h1><h2>Keyevent Overview</h2><p>Settings key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 176</code> and maps to the Android constant <code>KEYCODE_SETTINGS</code>.</p><h2>Behavior Details</h2><p>Settings key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
177:TV_POWER:TV power key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_POWER</h1><h2>Keyevent Overview</h2><p>TV power key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 177</code> and maps to the Android constant <code>KEYCODE_TV_POWER</code>.</p><h2>Behavior Details</h2><p>TV power key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
178:TV_INPUT:TV input key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT</h1><h2>Keyevent Overview</h2><p>TV input key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 178</code> and maps to the Android constant <code>KEYCODE_TV_INPUT</code>.</p><h2>Behavior Details</h2><p>TV input key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
179:STB_POWER:Set-top box power key. It is commonly found on TV remotes and set-top-box style devices.:<h1>STB_POWER</h1><h2>Keyevent Overview</h2><p>Set-top box power key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 179</code> and maps to the Android constant <code>KEYCODE_STB_POWER</code>.</p><h2>Behavior Details</h2><p>Set-top box power key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
180:STB_INPUT:Set-top box input key. It is commonly found on TV remotes and set-top-box style devices.:<h1>STB_INPUT</h1><h2>Keyevent Overview</h2><p>Set-top box input key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 180</code> and maps to the Android constant <code>KEYCODE_STB_INPUT</code>.</p><h2>Behavior Details</h2><p>Set-top box input key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
181:AVR_POWER:A/V receiver power key. It is commonly found on TV remotes and set-top-box style devices.:<h1>AVR_POWER</h1><h2>Keyevent Overview</h2><p>A/V receiver power key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 181</code> and maps to the Android constant <code>KEYCODE_AVR_POWER</code>.</p><h2>Behavior Details</h2><p>A/V receiver power key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
182:AVR_INPUT:A/V receiver input key. It is commonly found on TV remotes and set-top-box style devices.:<h1>AVR_INPUT</h1><h2>Keyevent Overview</h2><p>A/V receiver input key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 182</code> and maps to the Android constant <code>KEYCODE_AVR_INPUT</code>.</p><h2>Behavior Details</h2><p>A/V receiver input key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
183:PROG_RED:Red programmable key. It is commonly found on TV remotes and set-top-box style devices.:<h1>PROG_RED</h1><h2>Keyevent Overview</h2><p>Red programmable key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 183</code> and maps to the Android constant <code>KEYCODE_PROG_RED</code>.</p><h2>Behavior Details</h2><p>Red programmable key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
184:PROG_GREEN:Green programmable key. It is commonly found on TV remotes and set-top-box style devices.:<h1>PROG_GREEN</h1><h2>Keyevent Overview</h2><p>Green programmable key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 184</code> and maps to the Android constant <code>KEYCODE_PROG_GREEN</code>.</p><h2>Behavior Details</h2><p>Green programmable key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
185:PROG_YELLOW:Yellow programmable key. It is commonly found on TV remotes and set-top-box style devices.:<h1>PROG_YELLOW</h1><h2>Keyevent Overview</h2><p>Yellow programmable key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 185</code> and maps to the Android constant <code>KEYCODE_PROG_YELLOW</code>.</p><h2>Behavior Details</h2><p>Yellow programmable key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
186:PROG_BLUE:Blue programmable key. It is commonly found on TV remotes and set-top-box style devices.:<h1>PROG_BLUE</h1><h2>Keyevent Overview</h2><p>Blue programmable key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 186</code> and maps to the Android constant <code>KEYCODE_PROG_BLUE</code>.</p><h2>Behavior Details</h2><p>Blue programmable key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
187:APP_SWITCH:App switch key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>APP_SWITCH</h1><h2>Keyevent Overview</h2><p>App switch key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 187</code> and maps to the Android constant <code>KEYCODE_APP_SWITCH</code>.</p><h2>Behavior Details</h2><p>App switch key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
188:BUTTON_1:Generic game pad button 1. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_1</h1><h2>Keyevent Overview</h2><p>Generic game pad button 1. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 188</code> and maps to the Android constant <code>KEYCODE_BUTTON_1</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
189:BUTTON_2:Generic game pad button 2. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_2</h1><h2>Keyevent Overview</h2><p>Generic game pad button 2. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 189</code> and maps to the Android constant <code>KEYCODE_BUTTON_2</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
190:BUTTON_3:Generic game pad button 3. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_3</h1><h2>Keyevent Overview</h2><p>Generic game pad button 3. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 190</code> and maps to the Android constant <code>KEYCODE_BUTTON_3</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
191:BUTTON_4:Generic game pad button 4. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_4</h1><h2>Keyevent Overview</h2><p>Generic game pad button 4. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 191</code> and maps to the Android constant <code>KEYCODE_BUTTON_4</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 4 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
192:BUTTON_5:Generic game pad button 5. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_5</h1><h2>Keyevent Overview</h2><p>Generic game pad button 5. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 192</code> and maps to the Android constant <code>KEYCODE_BUTTON_5</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 5 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
193:BUTTON_6:Generic game pad button 6. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_6</h1><h2>Keyevent Overview</h2><p>Generic game pad button 6. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 193</code> and maps to the Android constant <code>KEYCODE_BUTTON_6</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 6 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
194:BUTTON_7:Generic game pad button 7. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_7</h1><h2>Keyevent Overview</h2><p>Generic game pad button 7. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 194</code> and maps to the Android constant <code>KEYCODE_BUTTON_7</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 7 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
195:BUTTON_8:Generic game pad button 8. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_8</h1><h2>Keyevent Overview</h2><p>Generic game pad button 8. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 195</code> and maps to the Android constant <code>KEYCODE_BUTTON_8</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 8 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
196:BUTTON_9:Generic game pad button 9. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_9</h1><h2>Keyevent Overview</h2><p>Generic game pad button 9. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 196</code> and maps to the Android constant <code>KEYCODE_BUTTON_9</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 9 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
197:BUTTON_10:Generic game pad button 10. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_10</h1><h2>Keyevent Overview</h2><p>Generic game pad button 10. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 197</code> and maps to the Android constant <code>KEYCODE_BUTTON_10</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 10 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
198:BUTTON_11:Generic game pad button 11. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_11</h1><h2>Keyevent Overview</h2><p>Generic game pad button 11. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 198</code> and maps to the Android constant <code>KEYCODE_BUTTON_11</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 11 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
199:BUTTON_12:Generic game pad button 12. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_12</h1><h2>Keyevent Overview</h2><p>Generic game pad button 12. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 199</code> and maps to the Android constant <code>KEYCODE_BUTTON_12</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 12 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
200:BUTTON_13:Generic game pad button 13. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_13</h1><h2>Keyevent Overview</h2><p>Generic game pad button 13. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 200</code> and maps to the Android constant <code>KEYCODE_BUTTON_13</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 13 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
201:BUTTON_14:Generic game pad button 14. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_14</h1><h2>Keyevent Overview</h2><p>Generic game pad button 14. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 201</code> and maps to the Android constant <code>KEYCODE_BUTTON_14</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 14 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
202:BUTTON_15:Generic game pad button 15. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_15</h1><h2>Keyevent Overview</h2><p>Generic game pad button 15. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 202</code> and maps to the Android constant <code>KEYCODE_BUTTON_15</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 15 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
203:BUTTON_16:Generic game pad button 16. It maps to a controller-style gamepad button on supported hardware.:<h1>BUTTON_16</h1><h2>Keyevent Overview</h2><p>Generic game pad button 16. It maps to a controller-style gamepad button on supported hardware.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 203</code> and maps to the Android constant <code>KEYCODE_BUTTON_16</code>.</p><h2>Behavior Details</h2><p>Generic game pad button 16 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
204:LANGUAGE_SWITCH:Language switch key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>LANGUAGE_SWITCH</h1><h2>Keyevent Overview</h2><p>Language switch key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 204</code> and maps to the Android constant <code>KEYCODE_LANGUAGE_SWITCH</code>.</p><h2>Behavior Details</h2><p>Language switch key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
205:MANNER_MODE:Manner mode key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MANNER_MODE</h1><h2>Keyevent Overview</h2><p>Manner mode key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 205</code> and maps to the Android constant <code>KEYCODE_MANNER_MODE</code>.</p><h2>Behavior Details</h2><p>Manner mode key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
206:3D_MODE:3D mode key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>3D_MODE</h1><h2>Keyevent Overview</h2><p>3D mode key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 206</code> and maps to the Android constant <code>KEYCODE_3D_MODE</code>.</p><h2>Behavior Details</h2><p>3D mode key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
207:CONTACTS:Contacts key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CONTACTS</h1><h2>Keyevent Overview</h2><p>Contacts key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 207</code> and maps to the Android constant <code>KEYCODE_CONTACTS</code>.</p><h2>Behavior Details</h2><p>Contacts key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
208:CALENDAR:Calendar key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CALENDAR</h1><h2>Keyevent Overview</h2><p>Calendar key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 208</code> and maps to the Android constant <code>KEYCODE_CALENDAR</code>.</p><h2>Behavior Details</h2><p>Calendar key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
209:MUSIC:Music key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MUSIC</h1><h2>Keyevent Overview</h2><p>Music key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 209</code> and maps to the Android constant <code>KEYCODE_MUSIC</code>.</p><h2>Behavior Details</h2><p>Music key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
210:CALCULATOR:Calculator key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CALCULATOR</h1><h2>Keyevent Overview</h2><p>Calculator key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 210</code> and maps to the Android constant <code>KEYCODE_CALCULATOR</code>.</p><h2>Behavior Details</h2><p>Calculator key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
211:ZENKAKU_HANKAKU:Japanese full-width / half-width key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ZENKAKU_HANKAKU</h1><h2>Keyevent Overview</h2><p>Japanese full-width / half-width key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 211</code> and maps to the Android constant <code>KEYCODE_ZENKAKU_HANKAKU</code>.</p><h2>Behavior Details</h2><p>Japanese full-width / half-width key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
212:EISU:Japanese alphanumeric key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>EISU</h1><h2>Keyevent Overview</h2><p>Japanese alphanumeric key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 212</code> and maps to the Android constant <code>KEYCODE_EISU</code>.</p><h2>Behavior Details</h2><p>Japanese alphanumeric key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
213:MUHENKAN:Japanese non-conversion key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MUHENKAN</h1><h2>Keyevent Overview</h2><p>Japanese non-conversion key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 213</code> and maps to the Android constant <code>KEYCODE_MUHENKAN</code>.</p><h2>Behavior Details</h2><p>Japanese non-conversion key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
214:HENKAN:Japanese conversion key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>HENKAN</h1><h2>Keyevent Overview</h2><p>Japanese conversion key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 214</code> and maps to the Android constant <code>KEYCODE_HENKAN</code>.</p><h2>Behavior Details</h2><p>Japanese conversion key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
215:KATAKANA_HIRAGANA:Japanese katakana / hiragana key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>KATAKANA_HIRAGANA</h1><h2>Keyevent Overview</h2><p>Japanese katakana / hiragana key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 215</code> and maps to the Android constant <code>KEYCODE_KATAKANA_HIRAGANA</code>.</p><h2>Behavior Details</h2><p>Japanese katakana / hiragana key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
216:YEN:Yen key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>YEN</h1><h2>Keyevent Overview</h2><p>Yen key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 216</code> and maps to the Android constant <code>KEYCODE_YEN</code>.</p><h2>Behavior Details</h2><p>Yen key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
217:RO:Japanese Ro key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>RO</h1><h2>Keyevent Overview</h2><p>Japanese Ro key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 217</code> and maps to the Android constant <code>KEYCODE_RO</code>.</p><h2>Behavior Details</h2><p>Japanese Ro key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
218:KANA:Japanese kana key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>KANA</h1><h2>Keyevent Overview</h2><p>Japanese kana key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 218</code> and maps to the Android constant <code>KEYCODE_KANA</code>.</p><h2>Behavior Details</h2><p>Japanese kana key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
219:ASSIST:Assist key. It is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>ASSIST</h1><h2>Keyevent Overview</h2><p>Assist key. It is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 219</code> and maps to the Android constant <code>KEYCODE_ASSIST</code>.</p><h2>Behavior Details</h2><p>Assist key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
220:BRIGHTNESS_DOWN:Brightness down key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>BRIGHTNESS_DOWN</h1><h2>Keyevent Overview</h2><p>Brightness down key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 220</code> and maps to the Android constant <code>KEYCODE_BRIGHTNESS_DOWN</code>.</p><h2>Behavior Details</h2><p>Brightness down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
221:BRIGHTNESS_UP:Brightness up key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>BRIGHTNESS_UP</h1><h2>Keyevent Overview</h2><p>Brightness up key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 221</code> and maps to the Android constant <code>KEYCODE_BRIGHTNESS_UP</code>.</p><h2>Behavior Details</h2><p>Brightness up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
222:MEDIA_AUDIO_TRACK:Audio track key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_AUDIO_TRACK</h1><h2>Keyevent Overview</h2><p>Audio track key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 222</code> and maps to the Android constant <code>KEYCODE_MEDIA_AUDIO_TRACK</code>.</p><h2>Behavior Details</h2><p>Audio track key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
223:SLEEP:Sleep key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SLEEP</h1><h2>Keyevent Overview</h2><p>Sleep key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 223</code> and maps to the Android constant <code>KEYCODE_SLEEP</code>.</p><h2>Behavior Details</h2><p>Sleep key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
224:WAKEUP:Wakeup key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>WAKEUP</h1><h2>Keyevent Overview</h2><p>Wakeup key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 224</code> and maps to the Android constant <code>KEYCODE_WAKEUP</code>.</p><h2>Behavior Details</h2><p>Wakeup key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
225:PAIRING:Pairing key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PAIRING</h1><h2>Keyevent Overview</h2><p>Pairing key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 225</code> and maps to the Android constant <code>KEYCODE_PAIRING</code>.</p><h2>Behavior Details</h2><p>Pairing key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
226:MEDIA_TOP_MENU:Media top menu key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_TOP_MENU</h1><h2>Keyevent Overview</h2><p>Media top menu key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 226</code> and maps to the Android constant <code>KEYCODE_MEDIA_TOP_MENU</code>.</p><h2>Behavior Details</h2><p>Media top menu key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
227:11:Number 11 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>11</h1><h2>Keyevent Overview</h2><p>Number 11 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 227</code> and maps to the Android constant <code>KEYCODE_11</code>.</p><h2>Behavior Details</h2><p>Number 11 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
228:12:Number 12 key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>12</h1><h2>Keyevent Overview</h2><p>Number 12 key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 228</code> and maps to the Android constant <code>KEYCODE_12</code>.</p><h2>Behavior Details</h2><p>Number 12 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
229:LAST_CHANNEL:Last channel key. It is commonly found on TV remotes and set-top-box style devices.:<h1>LAST_CHANNEL</h1><h2>Keyevent Overview</h2><p>Last channel key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 229</code> and maps to the Android constant <code>KEYCODE_LAST_CHANNEL</code>.</p><h2>Behavior Details</h2><p>Last channel key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
230:TV_DATA_SERVICE:TV data service key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_DATA_SERVICE</h1><h2>Keyevent Overview</h2><p>TV data service key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 230</code> and maps to the Android constant <code>KEYCODE_TV_DATA_SERVICE</code>.</p><h2>Behavior Details</h2><p>TV data service key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
231:VOICE_ASSIST:Voice assist key. It is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>VOICE_ASSIST</h1><h2>Keyevent Overview</h2><p>Voice assist key. It is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 231</code> and maps to the Android constant <code>KEYCODE_VOICE_ASSIST</code>.</p><h2>Behavior Details</h2><p>Voice assist key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
232:TV_RADIO_SERVICE:TV / radio service key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_RADIO_SERVICE</h1><h2>Keyevent Overview</h2><p>TV / radio service key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 232</code> and maps to the Android constant <code>KEYCODE_TV_RADIO_SERVICE</code>.</p><h2>Behavior Details</h2><p>TV / radio service key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
233:TV_TELETEXT:Teletext key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_TELETEXT</h1><h2>Keyevent Overview</h2><p>Teletext key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 233</code> and maps to the Android constant <code>KEYCODE_TV_TELETEXT</code>.</p><h2>Behavior Details</h2><p>Teletext key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
234:TV_NUMBER_ENTRY:TV number entry key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_NUMBER_ENTRY</h1><h2>Keyevent Overview</h2><p>TV number entry key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 234</code> and maps to the Android constant <code>KEYCODE_TV_NUMBER_ENTRY</code>.</p><h2>Behavior Details</h2><p>TV number entry key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
235:TV_TERRESTRIAL_ANALOG:Analog terrestrial TV key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_TERRESTRIAL_ANALOG</h1><h2>Keyevent Overview</h2><p>Analog terrestrial TV key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 235</code> and maps to the Android constant <code>KEYCODE_TV_TERRESTRIAL_ANALOG</code>.</p><h2>Behavior Details</h2><p>Analog terrestrial TV key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
236:TV_TERRESTRIAL_DIGITAL:Digital terrestrial TV key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_TERRESTRIAL_DIGITAL</h1><h2>Keyevent Overview</h2><p>Digital terrestrial TV key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 236</code> and maps to the Android constant <code>KEYCODE_TV_TERRESTRIAL_DIGITAL</code>.</p><h2>Behavior Details</h2><p>Digital terrestrial TV key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
237:TV_SATELLITE:Satellite TV key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_SATELLITE</h1><h2>Keyevent Overview</h2><p>Satellite TV key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 237</code> and maps to the Android constant <code>KEYCODE_TV_SATELLITE</code>.</p><h2>Behavior Details</h2><p>Satellite TV key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
238:TV_SATELLITE_BS:BS satellite TV key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_SATELLITE_BS</h1><h2>Keyevent Overview</h2><p>BS satellite TV key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 238</code> and maps to the Android constant <code>KEYCODE_TV_SATELLITE_BS</code>.</p><h2>Behavior Details</h2><p>BS satellite TV key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
239:TV_SATELLITE_CS:CS satellite TV key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_SATELLITE_CS</h1><h2>Keyevent Overview</h2><p>CS satellite TV key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 239</code> and maps to the Android constant <code>KEYCODE_TV_SATELLITE_CS</code>.</p><h2>Behavior Details</h2><p>CS satellite TV key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
240:TV_SATELLITE_SERVICE:BS / CS satellite service key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_SATELLITE_SERVICE</h1><h2>Keyevent Overview</h2><p>BS / CS satellite service key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 240</code> and maps to the Android constant <code>KEYCODE_TV_SATELLITE_SERVICE</code>.</p><h2>Behavior Details</h2><p>BS / CS satellite service key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
241:TV_NETWORK:TV network key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_NETWORK</h1><h2>Keyevent Overview</h2><p>TV network key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 241</code> and maps to the Android constant <code>KEYCODE_TV_NETWORK</code>.</p><h2>Behavior Details</h2><p>TV network key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
242:TV_ANTENNA_CABLE:Antenna / cable input key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_ANTENNA_CABLE</h1><h2>Keyevent Overview</h2><p>Antenna / cable input key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 242</code> and maps to the Android constant <code>KEYCODE_TV_ANTENNA_CABLE</code>.</p><h2>Behavior Details</h2><p>Antenna / cable input key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
243:TV_INPUT_HDMI_1:HDMI input 1 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_HDMI_1</h1><h2>Keyevent Overview</h2><p>HDMI input 1 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 243</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_HDMI_1</code>.</p><h2>Behavior Details</h2><p>HDMI input 1 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
244:TV_INPUT_HDMI_2:HDMI input 2 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_HDMI_2</h1><h2>Keyevent Overview</h2><p>HDMI input 2 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 244</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_HDMI_2</code>.</p><h2>Behavior Details</h2><p>HDMI input 2 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
245:TV_INPUT_HDMI_3:HDMI input 3 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_HDMI_3</h1><h2>Keyevent Overview</h2><p>HDMI input 3 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 245</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_HDMI_3</code>.</p><h2>Behavior Details</h2><p>HDMI input 3 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
246:TV_INPUT_HDMI_4:HDMI input 4 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_HDMI_4</h1><h2>Keyevent Overview</h2><p>HDMI input 4 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 246</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_HDMI_4</code>.</p><h2>Behavior Details</h2><p>HDMI input 4 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
247:TV_INPUT_COMPOSITE_1:Composite input 1 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_COMPOSITE_1</h1><h2>Keyevent Overview</h2><p>Composite input 1 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 247</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_COMPOSITE_1</code>.</p><h2>Behavior Details</h2><p>Composite input 1 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
248:TV_INPUT_COMPOSITE_2:Composite input 2 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_COMPOSITE_2</h1><h2>Keyevent Overview</h2><p>Composite input 2 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 248</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_COMPOSITE_2</code>.</p><h2>Behavior Details</h2><p>Composite input 2 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
249:TV_INPUT_COMPONENT_1:Component input 1 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_COMPONENT_1</h1><h2>Keyevent Overview</h2><p>Component input 1 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 249</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_COMPONENT_1</code>.</p><h2>Behavior Details</h2><p>Component input 1 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
250:TV_INPUT_COMPONENT_2:Component input 2 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_COMPONENT_2</h1><h2>Keyevent Overview</h2><p>Component input 2 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 250</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_COMPONENT_2</code>.</p><h2>Behavior Details</h2><p>Component input 2 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
251:TV_INPUT_VGA_1:VGA input 1 key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_INPUT_VGA_1</h1><h2>Keyevent Overview</h2><p>VGA input 1 key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 251</code> and maps to the Android constant <code>KEYCODE_TV_INPUT_VGA_1</code>.</p><h2>Behavior Details</h2><p>VGA input 1 key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
252:TV_AUDIO_DESCRIPTION:Audio description toggle key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_AUDIO_DESCRIPTION</h1><h2>Keyevent Overview</h2><p>Audio description toggle key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 252</code> and maps to the Android constant <code>KEYCODE_TV_AUDIO_DESCRIPTION</code>.</p><h2>Behavior Details</h2><p>Audio description toggle key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
253:TV_AUDIO_DESCRIPTION_MIX_UP:Audio-description volume up mix key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_AUDIO_DESCRIPTION_MIX_UP</h1><h2>Keyevent Overview</h2><p>Audio-description volume up mix key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 253</code> and maps to the Android constant <code>KEYCODE_TV_AUDIO_DESCRIPTION_MIX_UP</code>.</p><h2>Behavior Details</h2><p>Audio-description volume up mix key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
254:TV_AUDIO_DESCRIPTION_MIX_DOWN:Audio-description volume down mix key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_AUDIO_DESCRIPTION_MIX_DOWN</h1><h2>Keyevent Overview</h2><p>Audio-description volume down mix key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 254</code> and maps to the Android constant <code>KEYCODE_TV_AUDIO_DESCRIPTION_MIX_DOWN</code>.</p><h2>Behavior Details</h2><p>Audio-description volume down mix key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
255:TV_ZOOM_MODE:TV zoom mode key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_ZOOM_MODE</h1><h2>Keyevent Overview</h2><p>TV zoom mode key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 255</code> and maps to the Android constant <code>KEYCODE_TV_ZOOM_MODE</code>.</p><h2>Behavior Details</h2><p>TV zoom mode key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
256:TV_CONTENTS_MENU:TV contents menu key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_CONTENTS_MENU</h1><h2>Keyevent Overview</h2><p>TV contents menu key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 256</code> and maps to the Android constant <code>KEYCODE_TV_CONTENTS_MENU</code>.</p><h2>Behavior Details</h2><p>TV contents menu key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
257:TV_MEDIA_CONTEXT_MENU:TV media context menu key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_MEDIA_CONTEXT_MENU</h1><h2>Keyevent Overview</h2><p>TV media context menu key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 257</code> and maps to the Android constant <code>KEYCODE_TV_MEDIA_CONTEXT_MENU</code>.</p><h2>Behavior Details</h2><p>TV media context menu key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
258:TV_TIMER_PROGRAMMING:TV timer programming key. It is commonly found on TV remotes and set-top-box style devices.:<h1>TV_TIMER_PROGRAMMING</h1><h2>Keyevent Overview</h2><p>TV timer programming key. It is commonly found on TV remotes and set-top-box style devices.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 258</code> and maps to the Android constant <code>KEYCODE_TV_TIMER_PROGRAMMING</code>.</p><h2>Behavior Details</h2><p>TV timer programming key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
259:HELP:Help key. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>HELP</h1><h2>Keyevent Overview</h2><p>Help key. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 259</code> and maps to the Android constant <code>KEYCODE_HELP</code>.</p><h2>Behavior Details</h2><p>Help key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
260:NAVIGATE_PREVIOUS:Navigate previous key. It is used to move focus or navigate within interfaces.:<h1>NAVIGATE_PREVIOUS</h1><h2>Keyevent Overview</h2><p>Navigate previous key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 260</code> and maps to the Android constant <code>KEYCODE_NAVIGATE_PREVIOUS</code>.</p><h2>Behavior Details</h2><p>Navigate previous key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
261:NAVIGATE_NEXT:Navigate next key. It is used to move focus or navigate within interfaces.:<h1>NAVIGATE_NEXT</h1><h2>Keyevent Overview</h2><p>Navigate next key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 261</code> and maps to the Android constant <code>KEYCODE_NAVIGATE_NEXT</code>.</p><h2>Behavior Details</h2><p>Navigate next key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
262:NAVIGATE_IN:Navigate in key. It is used to move focus or navigate within interfaces.:<h1>NAVIGATE_IN</h1><h2>Keyevent Overview</h2><p>Navigate in key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 262</code> and maps to the Android constant <code>KEYCODE_NAVIGATE_IN</code>.</p><h2>Behavior Details</h2><p>Navigate in key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
263:NAVIGATE_OUT:Navigate out key. It is used to move focus or navigate within interfaces.:<h1>NAVIGATE_OUT</h1><h2>Keyevent Overview</h2><p>Navigate out key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 263</code> and maps to the Android constant <code>KEYCODE_NAVIGATE_OUT</code>.</p><h2>Behavior Details</h2><p>Navigate out key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
264:STEM_PRIMARY:Primary stem key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STEM_PRIMARY</h1><h2>Keyevent Overview</h2><p>Primary stem key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 264</code> and maps to the Android constant <code>KEYCODE_STEM_PRIMARY</code>.</p><h2>Behavior Details</h2><p>Primary stem key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
265:STEM_1:Stem key 1. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STEM_1</h1><h2>Keyevent Overview</h2><p>Stem key 1. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 265</code> and maps to the Android constant <code>KEYCODE_STEM_1</code>.</p><h2>Behavior Details</h2><p>Stem key 1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
266:STEM_2:Stem key 2. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STEM_2</h1><h2>Keyevent Overview</h2><p>Stem key 2. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 266</code> and maps to the Android constant <code>KEYCODE_STEM_2</code>.</p><h2>Behavior Details</h2><p>Stem key 2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
267:STEM_3:Stem key 3. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STEM_3</h1><h2>Keyevent Overview</h2><p>Stem key 3. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 267</code> and maps to the Android constant <code>KEYCODE_STEM_3</code>.</p><h2>Behavior Details</h2><p>Stem key 3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
268:DPAD_UP_LEFT:Directional pad up-left key. It is used to move focus or navigate within interfaces.:<h1>DPAD_UP_LEFT</h1><h2>Keyevent Overview</h2><p>Directional pad up-left key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 268</code> and maps to the Android constant <code>KEYCODE_DPAD_UP_LEFT</code>.</p><h2>Behavior Details</h2><p>Directional pad up-left key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
269:DPAD_DOWN_LEFT:Directional pad down-left key. It is used to move focus or navigate within interfaces.:<h1>DPAD_DOWN_LEFT</h1><h2>Keyevent Overview</h2><p>Directional pad down-left key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 269</code> and maps to the Android constant <code>KEYCODE_DPAD_DOWN_LEFT</code>.</p><h2>Behavior Details</h2><p>Directional pad down-left key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
270:DPAD_UP_RIGHT:Directional pad up-right key. It is used to move focus or navigate within interfaces.:<h1>DPAD_UP_RIGHT</h1><h2>Keyevent Overview</h2><p>Directional pad up-right key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 270</code> and maps to the Android constant <code>KEYCODE_DPAD_UP_RIGHT</code>.</p><h2>Behavior Details</h2><p>Directional pad up-right key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
271:DPAD_DOWN_RIGHT:Directional pad down-right key. It is used to move focus or navigate within interfaces.:<h1>DPAD_DOWN_RIGHT</h1><h2>Keyevent Overview</h2><p>Directional pad down-right key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 271</code> and maps to the Android constant <code>KEYCODE_DPAD_DOWN_RIGHT</code>.</p><h2>Behavior Details</h2><p>Directional pad down-right key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
272:MEDIA_SKIP_FORWARD:Skip forward media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_SKIP_FORWARD</h1><h2>Keyevent Overview</h2><p>Skip forward media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 272</code> and maps to the Android constant <code>KEYCODE_MEDIA_SKIP_FORWARD</code>.</p><h2>Behavior Details</h2><p>Skip forward media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
273:MEDIA_SKIP_BACKWARD:Skip backward media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_SKIP_BACKWARD</h1><h2>Keyevent Overview</h2><p>Skip backward media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 273</code> and maps to the Android constant <code>KEYCODE_MEDIA_SKIP_BACKWARD</code>.</p><h2>Behavior Details</h2><p>Skip backward media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
274:MEDIA_STEP_FORWARD:Step forward media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_STEP_FORWARD</h1><h2>Keyevent Overview</h2><p>Step forward media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 274</code> and maps to the Android constant <code>KEYCODE_MEDIA_STEP_FORWARD</code>.</p><h2>Behavior Details</h2><p>Step forward media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
275:MEDIA_STEP_BACKWARD:Step backward media key. It is mainly used for playback or call-control workflows.:<h1>MEDIA_STEP_BACKWARD</h1><h2>Keyevent Overview</h2><p>Step backward media key. It is mainly used for playback or call-control workflows.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 275</code> and maps to the Android constant <code>KEYCODE_MEDIA_STEP_BACKWARD</code>.</p><h2>Behavior Details</h2><p>Step backward media key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
276:SOFT_SLEEP:Soft sleep key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>SOFT_SLEEP</h1><h2>Keyevent Overview</h2><p>Soft sleep key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 276</code> and maps to the Android constant <code>KEYCODE_SOFT_SLEEP</code>.</p><h2>Behavior Details</h2><p>Soft sleep key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
277:CUT:Cut key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CUT</h1><h2>Keyevent Overview</h2><p>Cut key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 277</code> and maps to the Android constant <code>KEYCODE_CUT</code>.</p><h2>Behavior Details</h2><p>Cut key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
278:COPY:Copy key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>COPY</h1><h2>Keyevent Overview</h2><p>Copy key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 278</code> and maps to the Android constant <code>KEYCODE_COPY</code>.</p><h2>Behavior Details</h2><p>Copy key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
279:PASTE:Paste key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PASTE</h1><h2>Keyevent Overview</h2><p>Paste key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 279</code> and maps to the Android constant <code>KEYCODE_PASTE</code>.</p><h2>Behavior Details</h2><p>Paste key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
280:SYSTEM_NAVIGATION_UP:System navigation up key. It is used to move focus or navigate within interfaces.:<h1>SYSTEM_NAVIGATION_UP</h1><h2>Keyevent Overview</h2><p>System navigation up key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 280</code> and maps to the Android constant <code>KEYCODE_SYSTEM_NAVIGATION_UP</code>.</p><h2>Behavior Details</h2><p>System navigation up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
281:SYSTEM_NAVIGATION_DOWN:System navigation down key. It is used to move focus or navigate within interfaces.:<h1>SYSTEM_NAVIGATION_DOWN</h1><h2>Keyevent Overview</h2><p>System navigation down key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 281</code> and maps to the Android constant <code>KEYCODE_SYSTEM_NAVIGATION_DOWN</code>.</p><h2>Behavior Details</h2><p>System navigation down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
282:SYSTEM_NAVIGATION_LEFT:System navigation left key. It is used to move focus or navigate within interfaces.:<h1>SYSTEM_NAVIGATION_LEFT</h1><h2>Keyevent Overview</h2><p>System navigation left key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 282</code> and maps to the Android constant <code>KEYCODE_SYSTEM_NAVIGATION_LEFT</code>.</p><h2>Behavior Details</h2><p>System navigation left key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
283:SYSTEM_NAVIGATION_RIGHT:System navigation right key. It is used to move focus or navigate within interfaces.:<h1>SYSTEM_NAVIGATION_RIGHT</h1><h2>Keyevent Overview</h2><p>System navigation right key. It is used to move focus or navigate within interfaces.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 283</code> and maps to the Android constant <code>KEYCODE_SYSTEM_NAVIGATION_RIGHT</code>.</p><h2>Behavior Details</h2><p>System navigation right key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
284:ALL_APPS:All apps key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>ALL_APPS</h1><h2>Keyevent Overview</h2><p>All apps key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 284</code> and maps to the Android constant <code>KEYCODE_ALL_APPS</code>.</p><h2>Behavior Details</h2><p>All apps key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
285:REFRESH:Refresh key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>REFRESH</h1><h2>Keyevent Overview</h2><p>Refresh key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 285</code> and maps to the Android constant <code>KEYCODE_REFRESH</code>.</p><h2>Behavior Details</h2><p>Refresh key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
286:THUMBS_UP:Thumbs up key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>THUMBS_UP</h1><h2>Keyevent Overview</h2><p>Thumbs up key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 286</code> and maps to the Android constant <code>KEYCODE_THUMBS_UP</code>.</p><h2>Behavior Details</h2><p>Thumbs up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
287:THUMBS_DOWN:Thumbs down key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>THUMBS_DOWN</h1><h2>Keyevent Overview</h2><p>Thumbs down key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 287</code> and maps to the Android constant <code>KEYCODE_THUMBS_DOWN</code>.</p><h2>Behavior Details</h2><p>Thumbs down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
288:PROFILE_SWITCH:Profile switch key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>PROFILE_SWITCH</h1><h2>Keyevent Overview</h2><p>Profile switch key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 288</code> and maps to the Android constant <code>KEYCODE_PROFILE_SWITCH</code>.</p><h2>Behavior Details</h2><p>Profile switch key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
289:VIDEO_APP_1:Video application key 1. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_1</h1><h2>Keyevent Overview</h2><p>Video application key 1. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 289</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_1</code>.</p><h2>Behavior Details</h2><p>Video application key 1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
290:VIDEO_APP_2:Video application key 2. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_2</h1><h2>Keyevent Overview</h2><p>Video application key 2. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 290</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_2</code>.</p><h2>Behavior Details</h2><p>Video application key 2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
291:VIDEO_APP_3:Video application key 3. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_3</h1><h2>Keyevent Overview</h2><p>Video application key 3. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 291</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_3</code>.</p><h2>Behavior Details</h2><p>Video application key 3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
292:VIDEO_APP_4:Video application key 4. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_4</h1><h2>Keyevent Overview</h2><p>Video application key 4. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 292</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_4</code>.</p><h2>Behavior Details</h2><p>Video application key 4 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
293:VIDEO_APP_5:Video application key 5. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_5</h1><h2>Keyevent Overview</h2><p>Video application key 5. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 293</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_5</code>.</p><h2>Behavior Details</h2><p>Video application key 5 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
294:VIDEO_APP_6:Video application key 6. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_6</h1><h2>Keyevent Overview</h2><p>Video application key 6. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 294</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_6</code>.</p><h2>Behavior Details</h2><p>Video application key 6 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
295:VIDEO_APP_7:Video application key 7. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_7</h1><h2>Keyevent Overview</h2><p>Video application key 7. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 295</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_7</code>.</p><h2>Behavior Details</h2><p>Video application key 7 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
296:VIDEO_APP_8:Video application key 8. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>VIDEO_APP_8</h1><h2>Keyevent Overview</h2><p>Video application key 8. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 296</code> and maps to the Android constant <code>KEYCODE_VIDEO_APP_8</code>.</p><h2>Behavior Details</h2><p>Video application key 8 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
297:FEATURED_APP_1:Featured application key 1. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FEATURED_APP_1</h1><h2>Keyevent Overview</h2><p>Featured application key 1. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 297</code> and maps to the Android constant <code>KEYCODE_FEATURED_APP_1</code>.</p><h2>Behavior Details</h2><p>Featured application key 1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
298:FEATURED_APP_2:Featured application key 2. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FEATURED_APP_2</h1><h2>Keyevent Overview</h2><p>Featured application key 2. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 298</code> and maps to the Android constant <code>KEYCODE_FEATURED_APP_2</code>.</p><h2>Behavior Details</h2><p>Featured application key 2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
299:FEATURED_APP_3:Featured application key 3. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FEATURED_APP_3</h1><h2>Keyevent Overview</h2><p>Featured application key 3. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 299</code> and maps to the Android constant <code>KEYCODE_FEATURED_APP_3</code>.</p><h2>Behavior Details</h2><p>Featured application key 3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
300:FEATURED_APP_4:Featured application key 4. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FEATURED_APP_4</h1><h2>Keyevent Overview</h2><p>Featured application key 4. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 300</code> and maps to the Android constant <code>KEYCODE_FEATURED_APP_4</code>.</p><h2>Behavior Details</h2><p>Featured application key 4 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
301:DEMO_APP_1:Demo application key 1. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>DEMO_APP_1</h1><h2>Keyevent Overview</h2><p>Demo application key 1. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 301</code> and maps to the Android constant <code>KEYCODE_DEMO_APP_1</code>.</p><h2>Behavior Details</h2><p>Demo application key 1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
302:DEMO_APP_2:Demo application key 2. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>DEMO_APP_2</h1><h2>Keyevent Overview</h2><p>Demo application key 2. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 302</code> and maps to the Android constant <code>KEYCODE_DEMO_APP_2</code>.</p><h2>Behavior Details</h2><p>Demo application key 2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
303:DEMO_APP_3:Demo application key 3. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>DEMO_APP_3</h1><h2>Keyevent Overview</h2><p>Demo application key 3. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 303</code> and maps to the Android constant <code>KEYCODE_DEMO_APP_3</code>.</p><h2>Behavior Details</h2><p>Demo application key 3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
304:DEMO_APP_4:Demo application key 4. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>DEMO_APP_4</h1><h2>Keyevent Overview</h2><p>Demo application key 4. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 304</code> and maps to the Android constant <code>KEYCODE_DEMO_APP_4</code>.</p><h2>Behavior Details</h2><p>Demo application key 4 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
305:KEYBOARD_BACKLIGHT_DOWN:Keyboard backlight down key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>KEYBOARD_BACKLIGHT_DOWN</h1><h2>Keyevent Overview</h2><p>Keyboard backlight down key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 305</code> and maps to the Android constant <code>KEYCODE_KEYBOARD_BACKLIGHT_DOWN</code>.</p><h2>Behavior Details</h2><p>Keyboard backlight down key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
306:KEYBOARD_BACKLIGHT_UP:Keyboard backlight up key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>KEYBOARD_BACKLIGHT_UP</h1><h2>Keyevent Overview</h2><p>Keyboard backlight up key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 306</code> and maps to the Android constant <code>KEYCODE_KEYBOARD_BACKLIGHT_UP</code>.</p><h2>Behavior Details</h2><p>Keyboard backlight up key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
307:KEYBOARD_BACKLIGHT_TOGGLE:Keyboard backlight toggle key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>KEYBOARD_BACKLIGHT_TOGGLE</h1><h2>Keyevent Overview</h2><p>Keyboard backlight toggle key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 307</code> and maps to the Android constant <code>KEYCODE_KEYBOARD_BACKLIGHT_TOGGLE</code>.</p><h2>Behavior Details</h2><p>Keyboard backlight toggle key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
308:STYLUS_BUTTON_PRIMARY:Primary stylus barrel button. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STYLUS_BUTTON_PRIMARY</h1><h2>Keyevent Overview</h2><p>Primary stylus barrel button. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 308</code> and maps to the Android constant <code>KEYCODE_STYLUS_BUTTON_PRIMARY</code>.</p><h2>Behavior Details</h2><p>Primary stylus barrel button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
309:STYLUS_BUTTON_SECONDARY:Secondary stylus barrel button. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STYLUS_BUTTON_SECONDARY</h1><h2>Keyevent Overview</h2><p>Secondary stylus barrel button. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 309</code> and maps to the Android constant <code>KEYCODE_STYLUS_BUTTON_SECONDARY</code>.</p><h2>Behavior Details</h2><p>Secondary stylus barrel button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
310:STYLUS_BUTTON_TERTIARY:Tertiary stylus barrel button. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STYLUS_BUTTON_TERTIARY</h1><h2>Keyevent Overview</h2><p>Tertiary stylus barrel button. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 310</code> and maps to the Android constant <code>KEYCODE_STYLUS_BUTTON_TERTIARY</code>.</p><h2>Behavior Details</h2><p>Tertiary stylus barrel button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
311:STYLUS_BUTTON_TAIL:Stylus tail button. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>STYLUS_BUTTON_TAIL</h1><h2>Keyevent Overview</h2><p>Stylus tail button. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 311</code> and maps to the Android constant <code>KEYCODE_STYLUS_BUTTON_TAIL</code>.</p><h2>Behavior Details</h2><p>Stylus tail button This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
312:RECENT_APPS:Recent apps / overview key. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>RECENT_APPS</h1><h2>Keyevent Overview</h2><p>Recent apps / overview key. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 312</code> and maps to the Android constant <code>KEYCODE_RECENT_APPS</code>.</p><h2>Behavior Details</h2><p>Recent apps / overview key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
313:MACRO_1:User customizable macro key 1. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MACRO_1</h1><h2>Keyevent Overview</h2><p>User customizable macro key 1. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 313</code> and maps to the Android constant <code>KEYCODE_MACRO_1</code>.</p><h2>Behavior Details</h2><p>User customizable macro key 1 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
314:MACRO_2:User customizable macro key 2. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MACRO_2</h1><h2>Keyevent Overview</h2><p>User customizable macro key 2. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 314</code> and maps to the Android constant <code>KEYCODE_MACRO_2</code>.</p><h2>Behavior Details</h2><p>User customizable macro key 2 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
315:MACRO_3:User customizable macro key 3. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MACRO_3</h1><h2>Keyevent Overview</h2><p>User customizable macro key 3. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 315</code> and maps to the Android constant <code>KEYCODE_MACRO_3</code>.</p><h2>Behavior Details</h2><p>User customizable macro key 3 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
316:MACRO_4:User customizable macro key 4. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>MACRO_4</h1><h2>Keyevent Overview</h2><p>User customizable macro key 4. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 316</code> and maps to the Android constant <code>KEYCODE_MACRO_4</code>.</p><h2>Behavior Details</h2><p>User customizable macro key 4 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
317:EMOJI_PICKER:Emoji picker key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>EMOJI_PICKER</h1><h2>Keyevent Overview</h2><p>Emoji picker key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 317</code> and maps to the Android constant <code>KEYCODE_EMOJI_PICKER</code>.</p><h2>Behavior Details</h2><p>Emoji picker key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
318:SCREENSHOT:Screenshot key. This is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>SCREENSHOT</h1><h2>Keyevent Overview</h2><p>Screenshot key. This is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 318</code> and maps to the Android constant <code>KEYCODE_SCREENSHOT</code>.</p><h2>Behavior Details</h2><p>Screenshot key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
319:DICTATE:Dictation key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>DICTATE</h1><h2>Keyevent Overview</h2><p>Dictation key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 319</code> and maps to the Android constant <code>KEYCODE_DICTATE</code>.</p><h2>Behavior Details</h2><p>Dictation key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
320:NEW:New item / new window key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>NEW</h1><h2>Keyevent Overview</h2><p>New item / new window key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 320</code> and maps to the Android constant <code>KEYCODE_NEW</code>.</p><h2>Behavior Details</h2><p>New item / new window key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
321:CLOSE:Close item / close window key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>CLOSE</h1><h2>Keyevent Overview</h2><p>Close item / close window key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 321</code> and maps to the Android constant <code>KEYCODE_CLOSE</code>.</p><h2>Behavior Details</h2><p>Close item / close window key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
322:DO_NOT_DISTURB:Do Not Disturb toggle key. It is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>DO_NOT_DISTURB</h1><h2>Keyevent Overview</h2><p>Do Not Disturb toggle key. It is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 322</code> and maps to the Android constant <code>KEYCODE_DO_NOT_DISTURB</code>.</p><h2>Behavior Details</h2><p>Do Not Disturb toggle key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
323:PRINT:Print key. It is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>PRINT</h1><h2>Keyevent Overview</h2><p>Print key. It is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 323</code> and maps to the Android constant <code>KEYCODE_PRINT</code>.</p><h2>Behavior Details</h2><p>Print key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
324:LOCK:Screen lock key. It is often intercepted by the framework or the system UI on devices that reserve the action.:<h1>LOCK</h1><h2>Keyevent Overview</h2><p>Screen lock key. It is often intercepted by the framework or the system UI on devices that reserve the action.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 324</code> and maps to the Android constant <code>KEYCODE_LOCK</code>.</p><h2>Behavior Details</h2><p>Screen lock key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
325:FULLSCREEN:Fullscreen toggle key. It represents a distinct Android keycode that adb shell input can send to the system.:<h1>FULLSCREEN</h1><h2>Keyevent Overview</h2><p>Fullscreen toggle key. It represents a distinct Android keycode that adb shell input can send to the system.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 325</code> and maps to the Android constant <code>KEYCODE_FULLSCREEN</code>.</p><h2>Behavior Details</h2><p>Fullscreen toggle key This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
326:F13:Function key F13. It behaves like a standard function key on external keyboards.:<h1>F13</h1><h2>Keyevent Overview</h2><p>Function key F13. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 326</code> and maps to the Android constant <code>KEYCODE_F13</code>.</p><h2>Behavior Details</h2><p>Function key F13 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
327:F14:Function key F14. It behaves like a standard function key on external keyboards.:<h1>F14</h1><h2>Keyevent Overview</h2><p>Function key F14. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 327</code> and maps to the Android constant <code>KEYCODE_F14</code>.</p><h2>Behavior Details</h2><p>Function key F14 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
328:F15:Function key F15. It behaves like a standard function key on external keyboards.:<h1>F15</h1><h2>Keyevent Overview</h2><p>Function key F15. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 328</code> and maps to the Android constant <code>KEYCODE_F15</code>.</p><h2>Behavior Details</h2><p>Function key F15 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
329:F16:Function key F16. It behaves like a standard function key on external keyboards.:<h1>F16</h1><h2>Keyevent Overview</h2><p>Function key F16. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 329</code> and maps to the Android constant <code>KEYCODE_F16</code>.</p><h2>Behavior Details</h2><p>Function key F16 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
330:F17:Function key F17. It behaves like a standard function key on external keyboards.:<h1>F17</h1><h2>Keyevent Overview</h2><p>Function key F17. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 330</code> and maps to the Android constant <code>KEYCODE_F17</code>.</p><h2>Behavior Details</h2><p>Function key F17 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
331:F18:Function key F18. It behaves like a standard function key on external keyboards.:<h1>F18</h1><h2>Keyevent Overview</h2><p>Function key F18. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 331</code> and maps to the Android constant <code>KEYCODE_F18</code>.</p><h2>Behavior Details</h2><p>Function key F18 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
332:F19:Function key F19. It behaves like a standard function key on external keyboards.:<h1>F19</h1><h2>Keyevent Overview</h2><p>Function key F19. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 332</code> and maps to the Android constant <code>KEYCODE_F19</code>.</p><h2>Behavior Details</h2><p>Function key F19 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
333:F20:Function key F20. It behaves like a standard function key on external keyboards.:<h1>F20</h1><h2>Keyevent Overview</h2><p>Function key F20. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 333</code> and maps to the Android constant <code>KEYCODE_F20</code>.</p><h2>Behavior Details</h2><p>Function key F20 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
334:F21:Function key F21. It behaves like a standard function key on external keyboards.:<h1>F21</h1><h2>Keyevent Overview</h2><p>Function key F21. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 334</code> and maps to the Android constant <code>KEYCODE_F21</code>.</p><h2>Behavior Details</h2><p>Function key F21 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
335:F22:Function key F22. It behaves like a standard function key on external keyboards.:<h1>F22</h1><h2>Keyevent Overview</h2><p>Function key F22. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 335</code> and maps to the Android constant <code>KEYCODE_F22</code>.</p><h2>Behavior Details</h2><p>Function key F22 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
336:F23:Function key F23. It behaves like a standard function key on external keyboards.:<h1>F23</h1><h2>Keyevent Overview</h2><p>Function key F23. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 336</code> and maps to the Android constant <code>KEYCODE_F23</code>.</p><h2>Behavior Details</h2><p>Function key F23 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>
337:F24:Function key F24. It behaves like a standard function key on external keyboards.:<h1>F24</h1><h2>Keyevent Overview</h2><p>Function key F24. It behaves like a standard function key on external keyboards.</p><h2>ADB Command</h2><p>This keyevent is triggered with <code>adb shell input keyevent 337</code> and maps to the Android constant <code>KEYCODE_F24</code>.</p><h2>Behavior Details</h2><p>Function key F24 This event is sent through Android's input pipeline, so the final result can change depending on focused app, lock state, OEM customization, Android version, accessibility layers, and whether the system reserves or intercepts the action.</p><h2>Practical Notes</h2><p>Use this keyevent when you need repeatable automated navigation, device control, or UI testing. Always verify the behavior on the actual target device because some manufacturers remap, ignore, or override specific keyevents.</p>